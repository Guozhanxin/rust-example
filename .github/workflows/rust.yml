name: Rust build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - uses: icepuma/rust-action@master
      - name: Build
        shell: bash
        run: |
          cd hello_cargo
          cargo build